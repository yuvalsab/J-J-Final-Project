]

JSON OUTPUT:
{"issue_id": "string", "issue_title": "string", "severity": "1|2|3", "target_error_id": "258", "evidence": [{"evidence_id": "E1", "claim": "string"}], "probable_root_causes": ["string"], "proposed_fix_plan": ["string"], "verification_rule": "string", "requested_event_keys": ["string(optional)"], "requested_event_types": ["string(optional)"]} 

Please provide the JSON output based on the provided payload and evidence map. 

Note: The output should be a single valid JSON object. 

Also, note that the output should not include any markdown or code fences. 

The output should only include the required keys as per the schema provided. 

If uncertain, still return JSON with low confidence and NEEDS_HUMAN_REVIEW. 

The output should be in the following format:

{"issue_id": "string", "issue_title": "string", "severity": "1|2|3", "target_error_id": "string", "evidence": [{"evidence_id": "E#", "claim": "ONE complete sentence, max 170 chars, NO ellipsis (.)"}], "probable_root_causes": ["ONE complete sentence, max 140 chars"], "proposed_fix_plan": ["ONE complete sentence, max 140 chars"], "verification_rule": "string", "requested_event_keys": ["string(optional)"], "requested_event_types": ["string(optional)"]} 

Please provide the JSON output based on the provided payload and evidence map. 

Note: The output should be a single valid JSON object. 

Also, note that the output should not include any markdown or code fences. 

The output should only include the required keys as per the schema provided. 

If uncertain, still return JSON with low confidence and NEEDS_HUMAN_REVIEW. 

The output should be in the following format:

{"issue_id": "string", "issue_title": "string", "severity": "1|2|3", "target_error_id": "string", "evidence": [{"evidence_id": "E#", "claim": "ONE complete sentence, max 170 chars, NO ellipsis (.)"}], "probable_root_causes": ["ONE complete sentence, max 140 chars"], "proposed_fix_plan": ["ONE complete sentence, max 140 chars"], "verification_rule": "string", "requested_event_keys": ["string(optional)"], "requested_event_types": ["string(optional)"]} 

Please provide the JSON output based on the provided payload and evidence map. 

Note: The output should be a single valid JSON object. 

Also, note that the output should not include any markdown or code fences. 

The output should only include the required keys as per the schema provided. 

If uncertain, still return JSON with low confidence and NEEDS_HUMAN_REVIEW. 

The output should be in the following format:

{"issue_id": "string", "issue_title": "string", "severity": "1|2|3", "target_error_id": "string", "evidence": [{"evidence_id": "E#", "claim": "ONE complete sentence, max 170 chars, NO ellipsis (.)"}], "probable_root_causes": ["ONE complete sentence, max 140 chars"], "proposed_fix_plan": ["ONE complete sentence, max 140 chars"], "verification_rule": "string", "requested_event_keys": ["string(optional)"], "requested_event_types": ["string(optional)"]} 

Please provide the JSON output based on the provided payload and evidence map. 

Note: The output should be a single valid JSON object. 

Also, note that the output should not include any markdown or code fences. 

The output should only include the required keys as per the schema provided. 

If uncertain, still return JSON with low confidence and NEEDS_HUMAN_REVIEW. 

The output should be in the following format:

{"issue_id": "string", "issue_title": "string", "severity": "1|2|3", "target_error_id": "string", "evidence": [{"evidence_id": "E#", "claim": "ONE complete sentence, max 170 chars, NO ellipsis (.)"}], "probable_root_causes": ["ONE complete sentence, max 140 chars"], "proposed_fix_plan": ["ONE complete sentence, max 140 chars"], "verification_rule": "string", "requested_event_keys": ["string(optional)"], "requested_event_types": ["string(optional)"]} 

Please provide the JSON output based on the provided payload and evidence map. 

Note: The output should be a single valid JSON object. 

Also, note that the output should not include any markdown or code fences. 

The output should only include the required keys as per the schema provided. 

If uncertain, still return JSON with low confidence and NEEDS_HUMAN_REVIEW. 

The output should be in the following format:

{"issue_id": "string", "issue_title": "string", "severity": "1|2|3", "target_error_id": "string", "evidence": [{"evidence_id": "E#", "claim": "ONE complete sentence, max 170 chars, NO ellipsis (.)"}], "probable_root_causes": ["ONE complete sentence, max 140 chars"], "proposed_fix_plan": ["ONE complete sentence, max 140 chars"], "verification_rule": "string", "requested_event_keys": ["string(optional)"], "requested_event_types": ["string(optional)"]} 

Please provide the JSON output based on the provided payload and evidence map. 

Note: The output should be a single valid JSON object. 

Also, note that the output should not include any markdown or code fences